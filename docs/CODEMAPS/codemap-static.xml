<directory_structure>
docs/
patterns/
  p01/
    cmd/
      server/
        main.go
    internal/
      goroutine/
        consumer.go
        dispatcher.go
      worker/
        worker.go
  p02/
    cmd/
      api/
        main.go
      manager/
        main.go
      worker/
        main.go
    internal/
      rest/
        consumer.go
        dispatcher.go
  p03/
    cmd/
      api/
        main.go
      manager/
        main.go
      worker/
        main.go
    internal/
      websocket/
        consumer.go
        dispatcher.go
  p04/
    cmd/
      api/
        main.go
      manager/
        main.go
      worker/
        main.go
    internal/
      grpc/
        client.go
        consumer.go
        converter.go
        server.go
    proto/
      work_grpc.pb.go
      work.pb.go
  p05/
    cmd/
      api/
        main.go
      manager/
        main.go
      worker/
        main.go
    internal/
      nats/
        consumer.go
        dispatcher.go
        setup.go
      postgres/
        store.go
    nginx/
  p06/
    cmd/
      api/
        main.go
      manager/
        main.go
      worker/
        main.go
    internal/
      postgres/
        store.go
      pubsub/
        bridge.go
        consumer.go
        dispatcher.go
        setup.go
    nginx/
shared/
  api/
    handlers.go
    server.go
  client/
    manager.go
  contracts/
    consumer.go
    manager.go
    producer.go
  events/
    events.go
    memory.go
    nats.go
  executor/
    executor.go
  manager/
    manager.go
  models/
    task.go
  sse/
    client.go
    hub.go
  store/
    memory.go
    store.go
  templates/
    embed.go
tests/
  e2e/
    helpers_test.go
    task_test.go
  load/
    load.go
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="patterns/p01/internal/goroutine/consumer.go">
package goroutine
import (
	"context"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskConsumer = (*ChannelConsumer)(nil)
// ChannelConsumer is the worker-side view of the in-process channel transport.
// It reads tasks from the manager and writes all events back over a single channel.
type ChannelConsumer struct {
	tasks  <-chan models.Task      // read-only:  receive tasks from manager
	events chan<- models.TaskEvent // write-only: emit events to manager
}
⋮----
tasks  <-chan models.Task      // read-only:  receive tasks from manager
events chan<- models.TaskEvent // write-only: emit events to manager
⋮----
// Connect is a no-op; channels are ready at construction time.
func (c *ChannelConsumer) Connect(_ context.Context) error
// Receive blocks until a task is available or ctx is cancelled.
func (c *ChannelConsumer) Receive(ctx context.Context) (models.Task, error)
// Emit sends a TaskEvent to the manager. Terminal task_status events use a
// blocking send to guarantee delivery. All other events are best-effort and
// may be dropped if the channel is full.
func (c *ChannelConsumer) Emit(_ context.Context, event models.TaskEvent) error
⋮----
c.events <- event // blocking: must not lose terminal status
⋮----
default: // drop progress/running events if channel is full
</file>

<file path="patterns/p01/internal/goroutine/dispatcher.go">
package goroutine
import (
	"context"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskDispatcher = (*ChannelDispatcher)(nil)
// ChannelDispatcher is the manager-side view of the in-process channel transport.
// It writes tasks to workers and reads events back over a single ordered channel.
type ChannelDispatcher struct {
	tasks  chan<- models.Task      // write-only: dispatch tasks to workers
	events <-chan models.TaskEvent // read-only:  receive events from workers
}
⋮----
tasks  chan<- models.Task      // write-only: dispatch tasks to workers
events <-chan models.TaskEvent // read-only:  receive events from workers
⋮----
// New creates a linked ChannelDispatcher and ChannelConsumer sharing the same
// buffered channels. tasks is buffered to queueSize; events to 256.
func New(queueSize int) (*ChannelDispatcher, *ChannelConsumer)
// Start is a no-op; channels are ready at construction time.
func (p *ChannelDispatcher) Start(_ context.Context) error
// Dispatch sends the task to the tasks channel.
// Returns ErrDispatchFull immediately if the channel is at capacity.
func (p *ChannelDispatcher) Dispatch(_ context.Context, task models.Task) error
// ReceiveEvent blocks until an event is available or ctx is cancelled.
func (p *ChannelDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
</file>

<file path="patterns/p01/internal/worker/worker.go">
package worker
import (
	"context"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/executor"
⋮----
// RunWorker loops indefinitely, pulling tasks from consumer and executing them.
// The executor emits all status and progress events via consumer (TaskConsumer).
// Each call to RunWorker should run in its own goroutine.
func RunWorker(ctx context.Context, consumer contracts.TaskConsumer, exec *executor.Executor)
</file>

<file path="patterns/p02/cmd/api/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"github.com/kelseyhightower/envconfig"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/client"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"github.com/kelseyhightower/envconfig"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/client"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr       string `envconfig:"addr" default:":8080"`
	ManagerURL string `envconfig:"manager_url" default:"http://localhost:8081"`
}
func main()
⋮----
var cfg config
⋮----
// Pump manager's SSE stream into the local hub so browser clients connected
// to this API process receive real-time progress updates.
</file>

<file path="patterns/p02/cmd/worker/main.go">
package main
import (
	"context"
	"log"
	"os/signal"
	"syscall"
	"time"
	"github.com/kelseyhightower/envconfig"
	restinternal "work-distribution-patterns/patterns/p02/internal/rest"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"log"
"os/signal"
"syscall"
"time"
"github.com/kelseyhightower/envconfig"
restinternal "work-distribution-patterns/patterns/p02/internal/rest"
"work-distribution-patterns/shared/executor"
⋮----
type config struct {
	ManagerURL       string `envconfig:"manager_url" default:"http://localhost:8081"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
</file>

<file path="patterns/p02/internal/rest/consumer.go">
package rest
import (
	"bytes"
	"context"
	"encoding/json"
	"net/http"
	"time"
	"work-distribution-patterns/shared/models"
)
⋮----
"bytes"
"context"
"encoding/json"
"net/http"
"time"
"work-distribution-patterns/shared/models"
⋮----
// RESTConsumer implements contracts.TaskConsumer.
// It polls the manager's REST endpoints for tasks and emits events back.
type RESTConsumer struct {
	managerURL string
	httpClient *http.Client
}
// NewRESTConsumer creates a RESTConsumer that targets the given manager base URL.
func NewRESTConsumer(managerURL string) *RESTConsumer
// Connect implements contracts.TaskConsumer — no-op; polling starts in Receive.
func (c *RESTConsumer) Connect(_ context.Context) error
// Receive implements contracts.TaskConsumer.
// Polls GET /work/next: 204 → sleep 500 ms; 200 → decode and return; error → sleep 2 s.
// All sleeps respect ctx cancellation.
func (c *RESTConsumer) Receive(ctx context.Context) (models.Task, error)
⋮----
var task models.Task
⋮----
// Emit implements contracts.TaskConsumer.
// Posts the event as JSON to POST /work/events on the manager.
func (c *RESTConsumer) Emit(ctx context.Context, event models.TaskEvent) error
</file>

<file path="patterns/p02/internal/rest/dispatcher.go">
package rest
import (
	"context"
	"net/http"
	"github.com/labstack/echo/v4"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"net/http"
"github.com/labstack/echo/v4"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// RESTDispatcher implements contracts.TaskDispatcher.
// Workers poll GET /work/next for tasks and post events via POST /work/events.
type RESTDispatcher struct {
	tasks  chan models.Task
	events chan models.TaskEvent
}
// NewRESTDispatcher creates a RESTDispatcher with a buffered task queue.
func NewRESTDispatcher(queueSize int) *RESTDispatcher
// Start implements contracts.TaskDispatcher — no-op; no background initialisation needed.
func (p *RESTDispatcher) Start(_ context.Context) error
// Dispatch implements contracts.TaskDispatcher.
// Non-blocking: returns ErrDispatchFull if the queue is at capacity.
func (p *RESTDispatcher) Dispatch(_ context.Context, task models.Task) error
// ReceiveEvent implements contracts.TaskDispatcher.
// Blocks until an event arrives or ctx is cancelled.
func (p *RESTDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
// HandleNext handles GET /work/next.
// Returns 200+JSON if a task is queued, 204 if the queue is empty.
func (p *RESTDispatcher) HandleNext(c echo.Context) error
// HandleEvent handles POST /work/events.
// Terminal events (completed/failed) are forwarded with a blocking send to
// guarantee delivery; non-terminal events are best-effort.
func (p *RESTDispatcher) HandleEvent(c echo.Context) error
⋮----
var ev models.TaskEvent
⋮----
default: // drop non-terminal events if buffer is full
</file>

<file path="patterns/p03/cmd/api/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"github.com/kelseyhightower/envconfig"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/client"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"github.com/kelseyhightower/envconfig"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/client"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr       string `envconfig:"addr" default:":8080"`
	ManagerURL string `envconfig:"manager_url" default:"http://localhost:8081"`
}
func main()
⋮----
var cfg config
⋮----
// Pump manager's SSE stream into the local hub so browser clients connected
// to this API process receive real-time progress updates.
</file>

<file path="patterns/p03/cmd/worker/main.go">
package main
import (
	"context"
	"log"
	"os/signal"
	"syscall"
	"time"
	"github.com/kelseyhightower/envconfig"
	wsinternal "work-distribution-patterns/patterns/p03/internal/websocket"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"log"
"os/signal"
"syscall"
"time"
"github.com/kelseyhightower/envconfig"
wsinternal "work-distribution-patterns/patterns/p03/internal/websocket"
"work-distribution-patterns/shared/executor"
⋮----
type config struct {
	ManagerURL       string `envconfig:"manager_url" default:"ws://localhost:8081/ws/register"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
</file>

<file path="patterns/p03/internal/websocket/consumer.go">
package wsinternal
import (
	"context"
	"encoding/json"
	"log"
	"math"
	"sync"
	"time"
	"github.com/gorilla/websocket"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"log"
"math"
"sync"
"time"
"github.com/gorilla/websocket"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskConsumer = (*WebSocketConsumer)(nil)
// readyMsg is sent immediately after connecting to signal the worker is idle.
type readyMsg struct {
	Type string `json:"type"`
}
// WebSocketConsumer implements dispatch.TaskConsumer over a WebSocket connection
// to the API. Connect starts the reconnect loop (non-blocking); Receive blocks
// until a task arrives.
type WebSocketConsumer struct {
	apiURL      string
	tasks       chan models.Task
	mu          sync.Mutex
	currentSend chan []byte // guarded by mu; nil when disconnected
}
⋮----
currentSend chan []byte // guarded by mu; nil when disconnected
⋮----
// NewWebSocketConsumer creates a WebSocketConsumer that connects to the given URL.
func NewWebSocketConsumer(apiURL string) *WebSocketConsumer
// Connect starts the reconnect loop in a background goroutine (non-blocking).
func (c *WebSocketConsumer) Connect(ctx context.Context) error
func (c *WebSocketConsumer) reconnectLoop(ctx context.Context)
// Receive blocks until a task is available or ctx is cancelled.
func (c *WebSocketConsumer) Receive(ctx context.Context) (models.Task, error)
// Emit sends a TaskEvent to the API over WebSocket.
// Terminal task_status events use a blocking send to ensure delivery.
// All other events are best-effort and may be dropped if the send buffer is full.
func (c *WebSocketConsumer) Emit(_ context.Context, event models.TaskEvent) error
⋮----
send <- data // blocking: must not lose terminal status
⋮----
// runConn manages one connection lifecycle: write pump + read loop.
func (c *WebSocketConsumer) runConn(ctx context.Context, conn *websocket.Conn, send chan []byte) error
⋮----
// Write pump — only goroutine that writes to conn.
⋮----
var msg taskMsg
</file>

<file path="patterns/p03/internal/websocket/dispatcher.go">
package wsinternal
import (
	"context"
	"encoding/json"
	"log"
	"sync"
	"time"
	"github.com/google/uuid"
	"github.com/gorilla/websocket"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"log"
"sync"
"time"
"github.com/google/uuid"
"github.com/gorilla/websocket"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskDispatcher = (*WebSocketDispatcher)(nil)
// msgTypeTask is sent from the API to a worker carrying the task to execute.
const msgTypeTask = "task"
// taskMsg wraps a Task for delivery to a worker over WebSocket.
type taskMsg struct {
	Type string      `json:"type"`
	Task models.Task `json:"task"`
}
// workerConn represents a connected remote worker.
type workerConn struct {
	id   string
	conn *websocket.Conn
	send chan []byte
	hub  *WebSocketDispatcher
	busy bool
}
// WebSocketDispatcher manages connected remote workers and implements TaskDispatcher.
// Workers connect via HTTP GET /ws/register; Register is called by that handler.
type WebSocketDispatcher struct {
	mu      sync.Mutex
	workers []*workerConn
	nextIdx int
	events  chan models.TaskEvent
}
// NewWebSocketDispatcher creates a WebSocketDispatcher with a buffered event channel.
func NewWebSocketDispatcher() *WebSocketDispatcher
// Start is a no-op; subscriptions are established per-connection via Register.
func (p *WebSocketDispatcher) Start(_ context.Context) error
// Register adds a new worker connection and starts its read/write pumps.
func (p *WebSocketDispatcher) Register(conn *websocket.Conn)
// Dispatch sends the task to an idle worker using round-robin.
// Returns ErrNoWorkers if all workers are busy or none are connected.
func (p *WebSocketDispatcher) Dispatch(_ context.Context, task models.Task) error
// ReceiveEvent blocks until an event is available or ctx is cancelled.
func (p *WebSocketDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
func (p *WebSocketDispatcher) remove(wc *workerConn)
// writePump is the only goroutine that writes to the WebSocket connection.
func (wc *workerConn) writePump()
// readPump processes TaskEvent messages from the worker.
// Terminal task_status events mark the worker as idle and use a blocking send.
// All other events are forwarded best-effort.
func (wc *workerConn) readPump()
⋮----
var ev models.TaskEvent
⋮----
wc.hub.events <- ev // blocking: must not lose terminal status
⋮----
// Unknown types (e.g., "ready") are silently ignored.
</file>

<file path="patterns/p04/internal/grpc/client.go">
package grpc
import (
	"context"
	"errors"
	"io"
	"log"
	pb "work-distribution-patterns/patterns/p04/proto"
	"work-distribution-patterns/shared/models"
	"google.golang.org/grpc"
	"google.golang.org/grpc/credentials/insecure"
)
⋮----
"context"
"errors"
"io"
"log"
pb "work-distribution-patterns/patterns/p04/proto"
"work-distribution-patterns/shared/models"
"google.golang.org/grpc"
"google.golang.org/grpc/credentials/insecure"
⋮----
// Client implements contracts.TaskManager using gRPC
type Client struct {
	conn   *grpc.ClientConn
	client pb.TaskManagerClient
}
// NewClient creates a new gRPC client for the API
func NewClient(managerAddr string) (*Client, error)
// Submit creates a new task
func (c *Client) Submit(ctx context.Context, task models.Task) error
// Get retrieves a task by ID
func (c *Client) Get(ctx context.Context, id string) (models.Task, bool)
// List returns all tasks
func (c *Client) List(ctx context.Context) []models.Task
// Subscribe returns a channel of task events
func (c *Client) Subscribe(ctx context.Context, taskID string) (<-chan models.TaskEvent, error)
⋮----
var tid *string
⋮----
// Close closes the gRPC connection
func (c *Client) Close() error
</file>

<file path="patterns/p04/internal/grpc/consumer.go">
package grpc
import (
	"context"
	"errors"
	"io"
	"log"
	"sync"
	"time"
	pb "work-distribution-patterns/patterns/p04/proto"
	"work-distribution-patterns/shared/models"
	"google.golang.org/grpc"
	"google.golang.org/grpc/credentials/insecure"
)
⋮----
"context"
"errors"
"io"
"log"
"sync"
"time"
pb "work-distribution-patterns/patterns/p04/proto"
"work-distribution-patterns/shared/models"
"google.golang.org/grpc"
"google.golang.org/grpc/credentials/insecure"
⋮----
var errNotConnected = errors.New("not connected to manager")
// Consumer implements contracts.TaskConsumer using gRPC bidirectional streaming
type Consumer struct {
	managerAddr string
	conn        *grpc.ClientConn
	client      pb.WorkDistributionClient
	stream      pb.WorkDistribution_ConnectClient
	mu        sync.Mutex
	taskQueue chan models.Task
	connected bool
}
// NewConsumer creates a new gRPC consumer
func NewConsumer(managerAddr string) *Consumer
// Connect establishes a gRPC connection and starts the bidirectional stream
func (c *Consumer) Connect(ctx context.Context) error
⋮----
// Retry loop with backoff
⋮----
// Start receiving tasks
⋮----
// connect establishes the gRPC connection and stream
func (c *Consumer) connect(ctx context.Context) error
⋮----
// Connect to manager
⋮----
// Start bidirectional stream
⋮----
// receiveLoop receives tasks from the stream and queues them
func (c *Consumer) receiveLoop(ctx context.Context)
⋮----
// Convert and queue task
⋮----
// Receive returns the next task from the queue (blocks)
func (c *Consumer) Receive(ctx context.Context) (models.Task, error)
// Emit sends an event to the manager
func (c *Consumer) Emit(ctx context.Context, event models.TaskEvent) error
// Close closes the gRPC connection
func (c *Consumer) Close() error
</file>

<file path="patterns/p04/internal/grpc/converter.go">
package grpc
import (
	"time"
	pb "work-distribution-patterns/patterns/p04/proto"
	"work-distribution-patterns/shared/models"
)
⋮----
"time"
pb "work-distribution-patterns/patterns/p04/proto"
"work-distribution-patterns/shared/models"
⋮----
// TaskToProto converts models.Task to proto.Task
func TaskToProto(t models.Task) *pb.Task
// ProtoToTask converts proto.Task to models.Task
func ProtoToTask(pt *pb.Task) models.Task
⋮----
var dispatchedAt, completedAt *time.Time
⋮----
// EventToProto converts models.TaskEvent to proto.TaskEvent
func EventToProto(e models.TaskEvent) *pb.TaskEvent
// ProtoToEvent converts proto.TaskEvent to models.TaskEvent
func ProtoToEvent(pe *pb.TaskEvent) models.TaskEvent
</file>

<file path="patterns/p04/proto/work_grpc.pb.go">
// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v6.33.4
// source: patterns/p04/proto/work.proto
package gen
import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)
⋮----
context "context"
grpc "google.golang.org/grpc"
codes "google.golang.org/grpc/codes"
status "google.golang.org/grpc/status"
⋮----
// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9
const (
	WorkDistribution_Connect_FullMethodName = "/work.WorkDistribution/Connect"
)
// WorkDistributionClient is the client API for WorkDistribution service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
⋮----
// WorkDistribution service: Manager -> Worker bidirectional streaming
type WorkDistributionClient interface {
	// Connect establishes a bidirectional stream between Manager and Worker
	// Manager sends Tasks down; Worker sends TaskEvents up
	Connect(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[TaskEvent, Task], error)
}
⋮----
// Connect establishes a bidirectional stream between Manager and Worker
// Manager sends Tasks down; Worker sends TaskEvents up
⋮----
type workDistributionClient struct {
	cc grpc.ClientConnInterface
}
func NewWorkDistributionClient(cc grpc.ClientConnInterface) WorkDistributionClient
func (c *workDistributionClient) Connect(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[TaskEvent, Task], error)
// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
⋮----
// WorkDistributionServer is the server API for WorkDistribution service.
// All implementations must embed UnimplementedWorkDistributionServer
// for forward compatibility.
⋮----
type WorkDistributionServer interface {
	// Connect establishes a bidirectional stream between Manager and Worker
	// Manager sends Tasks down; Worker sends TaskEvents up
	Connect(grpc.BidiStreamingServer[TaskEvent, Task]) error
	mustEmbedUnimplementedWorkDistributionServer()
}
// UnimplementedWorkDistributionServer must be embedded to have
// forward compatible implementations.
⋮----
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedWorkDistributionServer struct{}
⋮----
func (UnimplementedWorkDistributionServer) mustEmbedUnimplementedWorkDistributionServer()
func (UnimplementedWorkDistributionServer) testEmbeddedByValue()
// UnsafeWorkDistributionServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to WorkDistributionServer will
// result in compilation errors.
type UnsafeWorkDistributionServer interface {
	mustEmbedUnimplementedWorkDistributionServer()
}
func RegisterWorkDistributionServer(s grpc.ServiceRegistrar, srv WorkDistributionServer)
⋮----
// If the following call panics, it indicates UnimplementedWorkDistributionServer was
// embedded by pointer and is nil.  This will cause panics if an
// unimplemented method is ever invoked, so we test this at initialization
// time to prevent it from happening at runtime later due to I/O.
⋮----
func _WorkDistribution_Connect_Handler(srv interface
⋮----
// WorkDistribution_ServiceDesc is the grpc.ServiceDesc for WorkDistribution service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var WorkDistribution_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "work.WorkDistribution",
	HandlerType: (*WorkDistributionServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Connect",
			Handler:       _WorkDistribution_Connect_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "patterns/p04/proto/work.proto",
}
const (
	TaskManager_Submit_FullMethodName    = "/work.TaskManager/Submit"
	TaskManager_Get_FullMethodName       = "/work.TaskManager/Get"
	TaskManager_List_FullMethodName      = "/work.TaskManager/List"
	TaskManager_Subscribe_FullMethodName = "/work.TaskManager/Subscribe"
)
// TaskManagerClient is the client API for TaskManager service.
⋮----
// TaskManager service: API -> Manager
type TaskManagerClient interface {
	// Submit creates a new task
	Submit(ctx context.Context, in *SubmitRequest, opts ...grpc.CallOption) (*SubmitResponse, error)
	// Get retrieves a task by ID
	Get(ctx context.Context, in *GetRequest, opts ...grpc.CallOption) (*GetResponse, error)
	// List returns all tasks
	List(ctx context.Context, in *ListRequest, opts ...grpc.CallOption) (*ListResponse, error)
	// Subscribe streams events for a specific task or all tasks
	Subscribe(ctx context.Context, in *SubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[TaskEvent], error)
}
⋮----
// Submit creates a new task
⋮----
// Get retrieves a task by ID
⋮----
// List returns all tasks
⋮----
// Subscribe streams events for a specific task or all tasks
⋮----
type taskManagerClient struct {
	cc grpc.ClientConnInterface
}
func NewTaskManagerClient(cc grpc.ClientConnInterface) TaskManagerClient
func (c *taskManagerClient) Submit(ctx context.Context, in *SubmitRequest, opts ...grpc.CallOption) (*SubmitResponse, error)
func (c *taskManagerClient) Get(ctx context.Context, in *GetRequest, opts ...grpc.CallOption) (*GetResponse, error)
func (c *taskManagerClient) List(ctx context.Context, in *ListRequest, opts ...grpc.CallOption) (*ListResponse, error)
func (c *taskManagerClient) Subscribe(ctx context.Context, in *SubscribeRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[TaskEvent], error)
⋮----
// TaskManagerServer is the server API for TaskManager service.
// All implementations must embed UnimplementedTaskManagerServer
⋮----
type TaskManagerServer interface {
	// Submit creates a new task
	Submit(context.Context, *SubmitRequest) (*SubmitResponse, error)
	// Get retrieves a task by ID
	Get(context.Context, *GetRequest) (*GetResponse, error)
	// List returns all tasks
	List(context.Context, *ListRequest) (*ListResponse, error)
	// Subscribe streams events for a specific task or all tasks
	Subscribe(*SubscribeRequest, grpc.ServerStreamingServer[TaskEvent]) error
	mustEmbedUnimplementedTaskManagerServer()
}
// UnimplementedTaskManagerServer must be embedded to have
⋮----
type UnimplementedTaskManagerServer struct{}
⋮----
func (UnimplementedTaskManagerServer) mustEmbedUnimplementedTaskManagerServer()
⋮----
// UnsafeTaskManagerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TaskManagerServer will
⋮----
type UnsafeTaskManagerServer interface {
	mustEmbedUnimplementedTaskManagerServer()
}
func RegisterTaskManagerServer(s grpc.ServiceRegistrar, srv TaskManagerServer)
⋮----
// If the following call panics, it indicates UnimplementedTaskManagerServer was
⋮----
func _TaskManager_Submit_Handler(srv interface
func _TaskManager_Get_Handler(srv interface
func _TaskManager_List_Handler(srv interface
func _TaskManager_Subscribe_Handler(srv interface
⋮----
// TaskManager_ServiceDesc is the grpc.ServiceDesc for TaskManager service.
⋮----
var TaskManager_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "work.TaskManager",
	HandlerType: (*TaskManagerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Submit",
			Handler:    _TaskManager_Submit_Handler,
		},
		{
			MethodName: "Get",
			Handler:    _TaskManager_Get_Handler,
		},
		{
			MethodName: "List",
			Handler:    _TaskManager_List_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Subscribe",
			Handler:       _TaskManager_Subscribe_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "patterns/p04/proto/work.proto",
}
</file>

<file path="patterns/p04/proto/work.pb.go">
// Code generated by protoc-gen-go. DO NOT EDIT.
// versions:
// 	protoc-gen-go v1.36.11
// 	protoc        v6.33.4
// source: patterns/p04/proto/work.proto
package gen
import (
	protoreflect "google.golang.org/protobuf/reflect/protoreflect"
	protoimpl "google.golang.org/protobuf/runtime/protoimpl"
	reflect "reflect"
	sync "sync"
	unsafe "unsafe"
)
⋮----
protoreflect "google.golang.org/protobuf/reflect/protoreflect"
protoimpl "google.golang.org/protobuf/runtime/protoimpl"
reflect "reflect"
sync "sync"
unsafe "unsafe"
⋮----
const (
	// Verify that this generated code is sufficiently up-to-date.
	_ = protoimpl.EnforceVersion(20 - protoimpl.MinVersion)
⋮----
// Verify that this generated code is sufficiently up-to-date.
⋮----
// Verify that runtime/protoimpl is sufficiently up-to-date.
⋮----
// Stage represents a single processing step within a task
type Stage struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Name          string                 `protobuf:"bytes,1,opt,name=name,proto3" json:"name,omitempty"`
	DurationMs    int32                  `protobuf:"varint,2,opt,name=duration_ms,json=durationMs,proto3" json:"duration_ms,omitempty"`
	Status        string                 `protobuf:"bytes,3,opt,name=status,proto3" json:"status,omitempty"`
	Error         *string                `protobuf:"bytes,4,opt,name=error,proto3,oneof" json:"error,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
func (x *Stage) Reset()
func (x *Stage) String() string
func (*Stage) ProtoMessage()
func (x *Stage) ProtoReflect() protoreflect.Message
// Deprecated: Use Stage.ProtoReflect.Descriptor instead.
func (*Stage) Descriptor() ([]byte, []int)
func (x *Stage) GetName() string
func (x *Stage) GetDurationMs() int32
func (x *Stage) GetStatus() string
func (x *Stage) GetError() string
// Task represents a work item
type Task struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Id            string                 `protobuf:"bytes,1,opt,name=id,proto3" json:"id,omitempty"`
	Title         string                 `protobuf:"bytes,2,opt,name=title,proto3" json:"title,omitempty"`
	Status        string                 `protobuf:"bytes,3,opt,name=status,proto3" json:"status,omitempty"`
	Stages        []*Stage               `protobuf:"bytes,4,rep,name=stages,proto3" json:"stages,omitempty"`
	CreatedAt     int64                  `protobuf:"varint,5,opt,name=created_at,json=createdAt,proto3" json:"created_at,omitempty"`
	UpdatedAt     int64                  `protobuf:"varint,6,opt,name=updated_at,json=updatedAt,proto3" json:"updated_at,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use Task.ProtoReflect.Descriptor instead.
⋮----
func (x *Task) GetId() string
func (x *Task) GetTitle() string
⋮----
func (x *Task) GetStages() []*Stage
func (x *Task) GetCreatedAt() int64
func (x *Task) GetUpdatedAt() int64
// TaskEvent represents a status update or progress event
type TaskEvent struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	TaskId        string                 `protobuf:"bytes,1,opt,name=task_id,json=taskId,proto3" json:"task_id,omitempty"`
	EventType     string                 `protobuf:"bytes,2,opt,name=event_type,json=eventType,proto3" json:"event_type,omitempty"`
	Status        string                 `protobuf:"bytes,3,opt,name=status,proto3" json:"status,omitempty"`
	StageName     *string                `protobuf:"bytes,4,opt,name=stage_name,json=stageName,proto3,oneof" json:"stage_name,omitempty"`
	Progress      *int32                 `protobuf:"varint,5,opt,name=progress,proto3,oneof" json:"progress,omitempty"`
	Error         *string                `protobuf:"bytes,6,opt,name=error,proto3,oneof" json:"error,omitempty"`
	Timestamp     int64                  `protobuf:"varint,7,opt,name=timestamp,proto3" json:"timestamp,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use TaskEvent.ProtoReflect.Descriptor instead.
⋮----
func (x *TaskEvent) GetTaskId() string
func (x *TaskEvent) GetEventType() string
⋮----
func (x *TaskEvent) GetStageName() string
func (x *TaskEvent) GetProgress() int32
⋮----
func (x *TaskEvent) GetTimestamp() int64
// Request/Response messages for TaskManager service (API -> Manager)
type SubmitRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Task          *Task                  `protobuf:"bytes,1,opt,name=task,proto3" json:"task,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use SubmitRequest.ProtoReflect.Descriptor instead.
⋮----
func (x *SubmitRequest) GetTask() *Task
type SubmitResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use SubmitResponse.ProtoReflect.Descriptor instead.
⋮----
type GetRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	TaskId        string                 `protobuf:"bytes,1,opt,name=task_id,json=taskId,proto3" json:"task_id,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use GetRequest.ProtoReflect.Descriptor instead.
⋮----
type GetResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Task          *Task                  `protobuf:"bytes,1,opt,name=task,proto3" json:"task,omitempty"`
	Found         bool                   `protobuf:"varint,2,opt,name=found,proto3" json:"found,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use GetResponse.ProtoReflect.Descriptor instead.
⋮----
func (x *GetResponse) GetFound() bool
type ListRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use ListRequest.ProtoReflect.Descriptor instead.
⋮----
type ListResponse struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	Tasks         []*Task                `protobuf:"bytes,1,rep,name=tasks,proto3" json:"tasks,omitempty"`
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
// Deprecated: Use ListResponse.ProtoReflect.Descriptor instead.
⋮----
func (x *ListResponse) GetTasks() []*Task
type SubscribeRequest struct {
	state         protoimpl.MessageState `protogen:"open.v1"`
	TaskId        *string                `protobuf:"bytes,1,opt,name=task_id,json=taskId,proto3,oneof" json:"task_id,omitempty"` // Empty for global subscription
	unknownFields protoimpl.UnknownFields
	sizeCache     protoimpl.SizeCache
}
⋮----
TaskId        *string                `protobuf:"bytes,1,opt,name=task_id,json=taskId,proto3,oneof" json:"task_id,omitempty"` // Empty for global subscription
⋮----
// Deprecated: Use SubscribeRequest.ProtoReflect.Descriptor instead.
⋮----
var File_patterns_p04_proto_work_proto protoreflect.FileDescriptor
const file_patterns_p04_proto_work_proto_rawDesc = "" +
	"\n" +
	"\x1dpatterns/p04/proto/work.proto\x12\x04work\"y\n" +
	"\x05Stage\x12\x12\n" +
	"\x04name\x18\x01 \x01(\tR\x04name\x12\x1f\n" +
	"\vduration_ms\x18\x02 \x01(\x05R\n" +
	"durationMs\x12\x16\n" +
	"\x06status\x18\x03 \x01(\tR\x06status\x12\x19\n" +
	"\x05error\x18\x04 \x01(\tH\x00R\x05error\x88\x01\x01B\b\n" +
	"\x06_error\"\xa7\x01\n" +
	"\x04Task\x12\x0e\n" +
	"\x02id\x18\x01 \x01(\tR\x02id\x12\x14\n" +
	"\x05title\x18\x02 \x01(\tR\x05title\x12\x16\n" +
	"\x06status\x18\x03 \x01(\tR\x06status\x12#\n" +
	"\x06stages\x18\x04 \x03(\v2\v.work.StageR\x06stages\x12\x1d\n" +
	"\n" +
	"created_at\x18\x05 \x01(\x03R\tcreatedAt\x12\x1d\n" +
	"\n" +
	"updated_at\x18\x06 \x01(\x03R\tupdatedAt\"\xff\x01\n" +
	"\tTaskEvent\x12\x17\n" +
	"\atask_id\x18\x01 \x01(\tR\x06taskId\x12\x1d\n" +
	"\n" +
	"event_type\x18\x02 \x01(\tR\teventType\x12\x16\n" +
	"\x06status\x18\x03 \x01(\tR\x06status\x12\"\n" +
	"\n" +
	"stage_name\x18\x04 \x01(\tH\x00R\tstageName\x88\x01\x01\x12\x1f\n" +
	"\bprogress\x18\x05 \x01(\x05H\x01R\bprogress\x88\x01\x01\x12\x19\n" +
	"\x05error\x18\x06 \x01(\tH\x02R\x05error\x88\x01\x01\x12\x1c\n" +
	"\ttimestamp\x18\a \x01(\x03R\ttimestampB\r\n" +
	"\v_stage_nameB\v\n" +
	"\t_progressB\b\n" +
	"\x06_error\"/\n" +
	"\rSubmitRequest\x12\x1e\n" +
	"\x04task\x18\x01 \x01(\v2\n" +
	".work.TaskR\x04task\"\x10\n" +
	"\x0eSubmitResponse\"%\n" +
	"\n" +
	"GetRequest\x12\x17\n" +
	"\atask_id\x18\x01 \x01(\tR\x06taskId\"C\n" +
	"\vGetResponse\x12\x1e\n" +
	"\x04task\x18\x01 \x01(\v2\n" +
	".work.TaskR\x04task\x12\x14\n" +
	"\x05found\x18\x02 \x01(\bR\x05found\"\r\n" +
	"\vListRequest\"0\n" +
	"\fListResponse\x12 \n" +
	"\x05tasks\x18\x01 \x03(\v2\n" +
	".work.TaskR\x05tasks\"<\n" +
	"\x10SubscribeRequest\x12\x1c\n" +
	"\atask_id\x18\x01 \x01(\tH\x00R\x06taskId\x88\x01\x01B\n" +
	"\n" +
	"\b_task_id2>\n" +
	"\x10WorkDistribution\x12*\n" +
	"\aConnect\x12\x0f.work.TaskEvent\x1a\n" +
	".work.Task(\x010\x012\xd5\x01\n" +
	"\vTaskManager\x123\n" +
	"\x06Submit\x12\x13.work.SubmitRequest\x1a\x14.work.SubmitResponse\x12*\n" +
	"\x03Get\x12\x10.work.GetRequest\x1a\x11.work.GetResponse\x12-\n" +
	"\x04List\x12\x11.work.ListRequest\x1a\x12.work.ListResponse\x126\n" +
	"\tSubscribe\x12\x16.work.SubscribeRequest\x1a\x0f.work.TaskEvent0\x01B7Z5work-distribution-patterns/patterns/p04/proto/gen;genb\x06proto3"
var (
	file_patterns_p04_proto_work_proto_rawDescOnce sync.Once
	file_patterns_p04_proto_work_proto_rawDescData []byte
)
func file_patterns_p04_proto_work_proto_rawDescGZIP() []byte
var file_patterns_p04_proto_work_proto_msgTypes = make([]protoimpl.MessageInfo, 10)
var file_patterns_p04_proto_work_proto_goTypes = []any{
	(*Stage)(nil),            // 0: work.Stage
	(*Task)(nil),             // 1: work.Task
	(*TaskEvent)(nil),        // 2: work.TaskEvent
	(*SubmitRequest)(nil),    // 3: work.SubmitRequest
	(*SubmitResponse)(nil),   // 4: work.SubmitResponse
	(*GetRequest)(nil),       // 5: work.GetRequest
	(*GetResponse)(nil),      // 6: work.GetResponse
	(*ListRequest)(nil),      // 7: work.ListRequest
	(*ListResponse)(nil),     // 8: work.ListResponse
	(*SubscribeRequest)(nil), // 9: work.SubscribeRequest
}
⋮----
(*Stage)(nil),            // 0: work.Stage
(*Task)(nil),             // 1: work.Task
(*TaskEvent)(nil),        // 2: work.TaskEvent
(*SubmitRequest)(nil),    // 3: work.SubmitRequest
(*SubmitResponse)(nil),   // 4: work.SubmitResponse
(*GetRequest)(nil),       // 5: work.GetRequest
(*GetResponse)(nil),      // 6: work.GetResponse
(*ListRequest)(nil),      // 7: work.ListRequest
(*ListResponse)(nil),     // 8: work.ListResponse
(*SubscribeRequest)(nil), // 9: work.SubscribeRequest
⋮----
var file_patterns_p04_proto_work_proto_depIdxs = []int32{
	0, // 0: work.Task.stages:type_name -> work.Stage
	1, // 1: work.SubmitRequest.task:type_name -> work.Task
	1, // 2: work.GetResponse.task:type_name -> work.Task
	1, // 3: work.ListResponse.tasks:type_name -> work.Task
	2, // 4: work.WorkDistribution.Connect:input_type -> work.TaskEvent
	3, // 5: work.TaskManager.Submit:input_type -> work.SubmitRequest
	5, // 6: work.TaskManager.Get:input_type -> work.GetRequest
	7, // 7: work.TaskManager.List:input_type -> work.ListRequest
	9, // 8: work.TaskManager.Subscribe:input_type -> work.SubscribeRequest
	1, // 9: work.WorkDistribution.Connect:output_type -> work.Task
	4, // 10: work.TaskManager.Submit:output_type -> work.SubmitResponse
	6, // 11: work.TaskManager.Get:output_type -> work.GetResponse
	8, // 12: work.TaskManager.List:output_type -> work.ListResponse
	2, // 13: work.TaskManager.Subscribe:output_type -> work.TaskEvent
	9, // [9:14] is the sub-list for method output_type
	4, // [4:9] is the sub-list for method input_type
	4, // [4:4] is the sub-list for extension type_name
	4, // [4:4] is the sub-list for extension extendee
	0, // [0:4] is the sub-list for field type_name
}
⋮----
0, // 0: work.Task.stages:type_name -> work.Stage
1, // 1: work.SubmitRequest.task:type_name -> work.Task
1, // 2: work.GetResponse.task:type_name -> work.Task
1, // 3: work.ListResponse.tasks:type_name -> work.Task
2, // 4: work.WorkDistribution.Connect:input_type -> work.TaskEvent
3, // 5: work.TaskManager.Submit:input_type -> work.SubmitRequest
5, // 6: work.TaskManager.Get:input_type -> work.GetRequest
7, // 7: work.TaskManager.List:input_type -> work.ListRequest
9, // 8: work.TaskManager.Subscribe:input_type -> work.SubscribeRequest
1, // 9: work.WorkDistribution.Connect:output_type -> work.Task
4, // 10: work.TaskManager.Submit:output_type -> work.SubmitResponse
6, // 11: work.TaskManager.Get:output_type -> work.GetResponse
8, // 12: work.TaskManager.List:output_type -> work.ListResponse
2, // 13: work.TaskManager.Subscribe:output_type -> work.TaskEvent
9, // [9:14] is the sub-list for method output_type
4, // [4:9] is the sub-list for method input_type
4, // [4:4] is the sub-list for extension type_name
4, // [4:4] is the sub-list for extension extendee
0, // [0:4] is the sub-list for field type_name
⋮----
func init()
func file_patterns_p04_proto_work_proto_init()
⋮----
type x struct{}
</file>

<file path="patterns/p05/cmd/worker/main.go">
package main
import (
	"context"
	"log"
	"os/signal"
	"syscall"
	"time"
	"github.com/kelseyhightower/envconfig"
	"github.com/nats-io/nats.go"
	natsinternal "work-distribution-patterns/patterns/p05/internal/nats"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"log"
"os/signal"
"syscall"
"time"
"github.com/kelseyhightower/envconfig"
"github.com/nats-io/nats.go"
natsinternal "work-distribution-patterns/patterns/p05/internal/nats"
"work-distribution-patterns/shared/executor"
⋮----
type config struct {
	NATSURL          string `envconfig:"nats_url" default:"nats://127.0.0.1:4222"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
⋮----
// Synchronous: exec.Run completes before we receive the next task,
// preserving NATS at-least-once delivery (ACK happens in Connect after
// the task is delivered to Receive).
</file>

<file path="patterns/p05/internal/nats/setup.go">
package natsinternal
import (
	"log"
	"github.com/nats-io/nats.go"
)
⋮----
"log"
"github.com/nats-io/nats.go"
⋮----
const (
	StreamName  = "TASKS"
	StreamSubj  = "tasks.>"
	ConsumerDur = "workers"
)
// SetupJetStream idempotently creates the TASKS stream.
// Safe to call from multiple services on startup.
func SetupJetStream(js nats.JetStreamContext) error
</file>

<file path="patterns/p05/internal/postgres/store.go">
package pgstore
import (
	"context"
	"encoding/json"
	"fmt"
	"time"
	"github.com/jackc/pgx/v5/pgxpool"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"fmt"
"time"
"github.com/jackc/pgx/v5/pgxpool"
"work-distribution-patterns/shared/models"
⋮----
const schema = `
CREATE TABLE IF NOT EXISTS tasks (
	id            TEXT        PRIMARY KEY,
	name          TEXT        NOT NULL,
	status        TEXT        NOT NULL,
	stages        JSONB       NOT NULL,
	submitted_at  TIMESTAMPTZ NOT NULL,
	dispatched_at TIMESTAMPTZ,
	completed_at  TIMESTAMPTZ
)`
// Store implements store.TaskStore against PostgreSQL.
type Store struct {
	pool *pgxpool.Pool
}
// New creates a Store and runs the schema migration.
func New(ctx context.Context, pool *pgxpool.Pool) (*Store, error)
func (s *Store) Create(task models.Task) error
func (s *Store) Get(id string) (models.Task, bool)
func (s *Store) List() []models.Task
⋮----
var tasks []models.Task
⋮----
func (s *Store) SetStatus(id string, status models.TaskStatus) error
⋮----
var completedAt *time.Time
⋮----
func (s *Store) SetDispatchedAt(id string, t time.Time) error
// scanner abstracts pgx.Row and pgx.Rows so scanTask works for both.
type scanner interface {
	Scan(dest ...any) error
}
func scanTask(s scanner) (models.Task, error)
⋮----
var t models.Task
var status string
var stagesJSON string
</file>

<file path="patterns/p06/internal/postgres/store.go">
package pgstore
import (
	"context"
	"encoding/json"
	"fmt"
	"time"
	"github.com/jackc/pgx/v5/pgxpool"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"fmt"
"time"
"github.com/jackc/pgx/v5/pgxpool"
"work-distribution-patterns/shared/models"
⋮----
const schema = `
CREATE TABLE IF NOT EXISTS tasks (
	id            TEXT        PRIMARY KEY,
	name          TEXT        NOT NULL,
	status        TEXT        NOT NULL,
	stages        JSONB       NOT NULL,
	submitted_at  TIMESTAMPTZ NOT NULL,
	dispatched_at TIMESTAMPTZ,
	completed_at  TIMESTAMPTZ
)`
// Store implements store.TaskStore against PostgreSQL.
type Store struct {
	pool *pgxpool.Pool
}
// New creates a Store and runs the schema migration.
func New(ctx context.Context, pool *pgxpool.Pool) (*Store, error)
func (s *Store) Create(task models.Task) error
func (s *Store) Get(id string) (models.Task, bool)
func (s *Store) List() []models.Task
⋮----
var tasks []models.Task
⋮----
func (s *Store) SetStatus(id string, status models.TaskStatus) error
⋮----
var completedAt *time.Time
⋮----
func (s *Store) SetDispatchedAt(id string, t time.Time) error
// scanner abstracts pgx.Row and pgx.Rows so scanTask works for both.
type scanner interface {
	Scan(dest ...any) error
}
func scanTask(s scanner) (models.Task, error)
⋮----
var t models.Task
var status string
var stagesJSON string
</file>

<file path="patterns/p06/internal/pubsub/bridge.go">
package pubsubinternal
import (
	"context"
	"encoding/json"
	"log"
	"work-distribution-patterns/shared/models"
	"gocloud.dev/pubsub"
)
⋮----
"context"
"encoding/json"
"log"
"work-distribution-patterns/shared/models"
"gocloud.dev/pubsub"
⋮----
// PubSubEventBridge implements TaskEventPublisher using Go Cloud PubSub.
type PubSubEventBridge struct {
	topic *pubsub.Topic
}
// NewPubSubEventBridge creates an event bridge from a pubsub.Topic.
func NewPubSubEventBridge(topic *pubsub.Topic) *PubSubEventBridge
// Publish sends an event to the topic (for manager→API event broadcasting).
func (b *PubSubEventBridge) Publish(event models.TaskEvent)
</file>

<file path="patterns/p06/internal/pubsub/consumer.go">
package pubsubinternal
import (
	"context"
	"encoding/json"
	"log"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
	"gocloud.dev/pubsub"
)
⋮----
"context"
"encoding/json"
"log"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
"gocloud.dev/pubsub"
⋮----
// Compile-time interface check.
var _ dispatch.TaskConsumer = (*PubSubConsumer)(nil)
// PubSubConsumer implements TaskConsumer over Go Cloud PubSub.
type PubSubConsumer struct {
	tasksSub    *pubsub.Subscription
	eventsTopic *pubsub.Topic
}
// NewPubSubConsumer creates a PubSubConsumer.
func NewPubSubConsumer(tasksSub *pubsub.Subscription, eventsTopic *pubsub.Topic) *PubSubConsumer
// Connect is a no-op as Go Cloud handles connection state internally.
func (c *PubSubConsumer) Connect(_ context.Context) error
// Receive blocks until a task is available from the subscription.
func (c *PubSubConsumer) Receive(ctx context.Context) (models.Task, error)
⋮----
var task models.Task
⋮----
msg.Ack() // Ack bad messages so they don't clog the queue.
⋮----
// For work distribution, we acknowledge after receiving.
// In a real system, we might want to use at-least-once delivery with
// acknowledgement only after successful processing, but following
// the pattern in P5 for consistency.
⋮----
// Emit publishes a TaskEvent to the events topic.
func (c *PubSubConsumer) Emit(ctx context.Context, event models.TaskEvent) error
// Shutdown closes the underlying Go Cloud resources.
func (c *PubSubConsumer) Shutdown(ctx context.Context)
</file>

<file path="patterns/p06/internal/pubsub/dispatcher.go">
package pubsubinternal
import (
	"context"
	"encoding/json"
	"log"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
	"gocloud.dev/pubsub"
)
⋮----
"context"
"encoding/json"
"log"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
"gocloud.dev/pubsub"
⋮----
// Compile-time interface check.
var _ dispatch.TaskDispatcher = (*PubSubDispatcher)(nil)
// PubSubDispatcher implements TaskDispatcher using Go Cloud PubSub topics and subscriptions.
type PubSubDispatcher struct {
	tasksTopic     *pubsub.Topic
	eventsSub      *pubsub.Subscription
	internalEvents chan models.TaskEvent
}
// NewPubSubDispatcher creates a PubSubDispatcher.
func NewPubSubDispatcher(tasksTopic *pubsub.Topic, eventsSub *pubsub.Subscription) *PubSubDispatcher
// Start launches a background goroutine to drain the events subscription.
func (d *PubSubDispatcher) Start(ctx context.Context) error
func (d *PubSubDispatcher) receiveLoop(ctx context.Context)
⋮----
// Context cancellation or subscription error.
⋮----
var ev models.TaskEvent
⋮----
// Acknowledge receipt.
⋮----
// Block briefly for terminal events to ensure they are processed.
⋮----
// Non-blocking for progress updates.
⋮----
// Dispatch publishes the task to the tasks topic.
func (d *PubSubDispatcher) Dispatch(ctx context.Context, task models.Task) error
// ReceiveEvent blocks until a worker event is available.
func (d *PubSubDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
// Shutdown closes the underlying Go Cloud resources.
func (d *PubSubDispatcher) Shutdown(ctx context.Context)
</file>

<file path="shared/sse/client.go">
package sse
import (
	"bufio"
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"strings"
	"time"
	"work-distribution-patterns/shared/models"
)
⋮----
"bufio"
"context"
"encoding/json"
"fmt"
"net/http"
"strings"
"time"
"work-distribution-patterns/shared/models"
⋮----
// Client connects to a remote SSE endpoint and streams TaskEvents.
// Used by P2/P3 API processes to subscribe to manager's event stream.
type Client struct {
	url        string
	httpClient *http.Client
}
// NewClient creates an SSE client that connects to the given URL.
func NewClient(url string) *Client
⋮----
Timeout: 0, // No timeout for SSE streaming
⋮----
// Subscribe connects to the SSE endpoint and returns a channel of events.
// The connection is maintained until ctx is cancelled.
// Automatically reconnects on disconnection with exponential backoff.
func (c *Client) Subscribe(ctx context.Context) (<-chan models.TaskEvent, error)
func (c *Client) streamWithReconnect(ctx context.Context, out chan<- models.TaskEvent)
⋮----
// Connection dropped, reconnect with backoff
⋮----
func (c *Client) streamEvents(ctx context.Context, out chan<- models.TaskEvent) error
⋮----
var ev models.TaskEvent
⋮----
continue // Skip malformed events
</file>

<file path="shared/templates/embed.go">
package templates
import "embed"
//go:embed index.html
var FS embed.FS
</file>

<file path="patterns/p04/cmd/api/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"github.com/kelseyhightower/envconfig"
	grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"github.com/kelseyhightower/envconfig"
grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr            string `envconfig:"addr" default:":8080"`
	ManagerGRPCAddr string `envconfig:"manager_grpc_addr" default:"localhost:9091"`
}
func main()
⋮----
var cfg config
⋮----
// Connect to manager via gRPC
⋮----
// Subscribe to all events from manager via gRPC and pump into local SSE hub
</file>

<file path="patterns/p04/cmd/worker/main.go">
package main
import (
	"context"
	"log"
	"os/signal"
	"syscall"
	"time"
	"github.com/kelseyhightower/envconfig"
	grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"log"
"os/signal"
"syscall"
"time"
"github.com/kelseyhightower/envconfig"
grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
"work-distribution-patterns/shared/executor"
⋮----
type config struct {
	ManagerGRPCAddr  string `envconfig:"manager_grpc_addr" default:"localhost:9091"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
</file>

<file path="patterns/p04/internal/grpc/server.go">
package grpc
import (
	"context"
	"errors"
	"fmt"
	"io"
	"log"
	"sync"
	pb "work-distribution-patterns/patterns/p04/proto"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/store"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/status"
)
⋮----
"context"
"errors"
"fmt"
"io"
"log"
"sync"
pb "work-distribution-patterns/patterns/p04/proto"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/store"
"google.golang.org/grpc/codes"
"google.golang.org/grpc/status"
⋮----
// Server implements both WorkDistributionServer and TaskManagerServer
type Server struct {
	pb.UnimplementedWorkDistributionServer
	pb.UnimplementedTaskManagerServer
	store    store.TaskStore
	eventBus events.TaskEventSubscriber
	// Worker management
	mu             sync.RWMutex
	workerStreams  map[string]pb.WorkDistribution_ConnectServer
	availableQueue chan string // worker IDs
	pendingTasks   chan models.Task
	// Event subscriptions
	subMu       sync.RWMutex
	subscribers map[string]chan models.TaskEvent
	nextSubID   int
	// Events from workers (for manager's event loop)
	workerEvents chan models.TaskEvent
}
⋮----
// Worker management
⋮----
availableQueue chan string // worker IDs
⋮----
// Event subscriptions
⋮----
// Events from workers (for manager's event loop)
⋮----
// NewServer creates a new gRPC server
func NewServer(st store.TaskStore, evBus events.TaskEventSubscriber) *Server
// Start begins background goroutines for dispatching and event broadcasting
func (s *Server) Start(ctx context.Context)
// dispatchLoop sends pending tasks to available workers
func (s *Server) dispatchLoop(ctx context.Context)
⋮----
// Wait for an available worker
⋮----
// Worker disconnected, re-queue task
⋮----
// Send task to worker
⋮----
// Worker failed, re-queue task and remove worker
⋮----
// eventBroadcastLoop forwards events from eventBus to gRPC subscribers
func (s *Server) eventBroadcastLoop(ctx context.Context)
⋮----
// Subscriber can't keep up, skip
⋮----
// removeWorker removes a worker from the pool
func (s *Server) removeWorker(workerID string)
// WorkDistribution.Connect - bidirectional stream for worker connection
func (s *Server) Connect(stream pb.WorkDistribution_ConnectServer) error
⋮----
// Generate worker ID
⋮----
// Register worker
⋮----
// Signal worker is available
⋮----
// Handle incoming events from worker
⋮----
// Convert and forward event to manager's event loop
⋮----
// For terminal events, mark worker as available again
⋮----
// Worker finished a task, mark as available
⋮----
// Queue full, skip
⋮----
// TaskManager.Submit - creates a new task
func (s *Server) Submit(ctx context.Context, req *pb.SubmitRequest) (*pb.SubmitResponse, error)
⋮----
// Store task
⋮----
// Enqueue for dispatch
⋮----
// TaskManager.Get - retrieves a task by ID
func (s *Server) Get(_ context.Context, req *pb.GetRequest) (*pb.GetResponse, error)
// TaskManager.List - returns all tasks
func (s *Server) List(_ context.Context, _ *pb.ListRequest) (*pb.ListResponse, error)
// TaskManager.Subscribe - streams events to API clients
func (s *Server) Subscribe(req *pb.SubscribeRequest, stream pb.TaskManager_SubscribeServer) error
⋮----
// Generate subscription ID
⋮----
// Stream events
⋮----
// Filter by task ID if specified
⋮----
// GRPCDispatcher implements contracts.TaskDispatcher for the Manager
type GRPCDispatcher struct {
	server *Server
}
// NewDispatcher creates a new GRPCDispatcher
func NewDispatcher(server *Server) *GRPCDispatcher
// Start starts the dispatcher (called by manager.Manager)
⋮----
// Dispatch enqueues a task for workers
func (d *GRPCDispatcher) Dispatch(ctx context.Context, task models.Task) error
// ReceiveEvent returns events from workers (blocks until event is available)
func (d *GRPCDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
</file>

<file path="patterns/p05/internal/nats/consumer.go">
package natsinternal
import (
	"context"
	"encoding/json"
	"log"
	"github.com/nats-io/nats.go"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"log"
"github.com/nats-io/nats.go"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskConsumer = (*NATSConsumer)(nil)
// NATSConsumer implements TaskConsumer over NATS.
// It receives tasks from JetStream (queue-subscribe with manual ACK for
// at-least-once delivery) and emits all task events via NATS Core.
// The worker loop is synchronous — one task at a time — preserving NATS
// at-least-once delivery semantics.
type NATSConsumer struct {
	nc    *nats.Conn
	js    nats.JetStreamContext
	tasks chan models.Task
}
// NewNATSConsumer creates a NATSConsumer. Call Connect to start the subscription.
func NewNATSConsumer(nc *nats.Conn, js nats.JetStreamContext) *NATSConsumer
// Connect starts the JetStream queue subscription in a background goroutine (non-blocking).
func (s *NATSConsumer) Connect(ctx context.Context) error
func (s *NATSConsumer) subscribe(ctx context.Context)
⋮----
var task models.Task
⋮----
// Block until the main loop picks up the task, then ACK.
// If the process crashes before Receive returns, NATS redelivers.
⋮----
// Receive blocks until a task is available or ctx is cancelled.
func (s *NATSConsumer) Receive(ctx context.Context) (models.Task, error)
// Emit publishes a TaskEvent to "worker.events.<taskID>" via NATS Core.
// The Manager subscribes to this subject, persists the event, then republishes
// it to the API-facing event bus.
func (s *NATSConsumer) Emit(_ context.Context, event models.TaskEvent) error
</file>

<file path="patterns/p05/internal/nats/dispatcher.go">
package natsinternal
import (
	"context"
	"encoding/json"
	"log"
	"github.com/nats-io/nats.go"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"encoding/json"
"log"
"github.com/nats-io/nats.go"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Compile-time interface check.
var _ dispatch.TaskDispatcher = (*NATSDispatcher)(nil)
// NATSDispatcher implements TaskDispatcher using NATS JetStream for dispatch and
// NATS Core for receiving all task events from workers via a single subject.
// Every API replica subscribes to task.events.*, but workers emit to
// worker.events.*. The Manager acts as a gateway, processing worker events
// before republishing them to the API-facing subject.
type NATSDispatcher struct {
	nc     *nats.Conn
	js     nats.JetStreamContext
	events chan models.TaskEvent
}
// NewNATSDispatcher creates a NATSDispatcher. Call Start to register NATS Core subscriptions.
func NewNATSDispatcher(nc *nats.Conn, js nats.JetStreamContext) *NATSDispatcher
// Start registers a single NATS Core subscription for all worker events.
// It is non-blocking; the subscription callback pushes events into the internal channel.
func (b *NATSDispatcher) Start(_ context.Context) error
⋮----
var ev models.TaskEvent
⋮----
b.events <- ev // blocking: must not lose terminal status
⋮----
// Dispatch publishes the task to the "tasks.new" JetStream subject.
func (b *NATSDispatcher) Dispatch(_ context.Context, task models.Task) error
// ReceiveEvent blocks until an event is available or ctx is cancelled.
func (b *NATSDispatcher) ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
</file>

<file path="patterns/p06/cmd/api/main.go">
package main
import (
	"context"
	"encoding/json"
	"html/template"
	"log"
	"github.com/kelseyhightower/envconfig"
	pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/client"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"encoding/json"
"html/template"
"log"
"github.com/kelseyhightower/envconfig"
pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/client"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr       string `envconfig:"addr" default:":8080"`
	ManagerURL string `envconfig:"manager_url" default:"http://localhost:8081"`
	BrokerURL  string `envconfig:"broker_url" default:"nats://localhost:4222"`
}
func main()
⋮----
var cfg config
⋮----
// 1. Subscribe to broker events (Go Cloud PubSub)
⋮----
// 2. Use RemoteTaskManager to proxy Submit/Get/List to the Manager
⋮----
// 3. Subscribe to broker events via Go Cloud and pump to SSE Hub
⋮----
var ev models.TaskEvent
</file>

<file path="patterns/p06/cmd/manager/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"net/http"
	"time"
	"github.com/jackc/pgx/v5/pgxpool"
	"github.com/kelseyhightower/envconfig"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	pgstore "work-distribution-patterns/patterns/p06/internal/postgres"
	pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"net/http"
"time"
"github.com/jackc/pgx/v5/pgxpool"
"github.com/kelseyhightower/envconfig"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
pgstore "work-distribution-patterns/patterns/p06/internal/postgres"
pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr        string `envconfig:"addr" default:":8081"`
	BrokerURL   string `envconfig:"broker_url" default:"nats://localhost:4222"`
	DatabaseURL string `envconfig:"database_url" default:"postgres://tasks:tasks@localhost:5432/tasks?sslmode=disable"`
}
func main()
⋮----
var cfg config
⋮----
// 1. Setup Postgres
⋮----
// 2. Setup PubSub (Go Cloud with NATS/RabbitMQ)
⋮----
// Start dispatcher to receive worker events
⋮----
// Event bridge for publishing to APIs (manager republishes worker events)
⋮----
// 4. Setup Manager
⋮----
// 5. Serve Manager API
⋮----
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck
⋮----
var task models.Task
</file>

<file path="patterns/p06/cmd/worker/main.go">
package main
import (
	"context"
	"log"
	"net/http"
	"os/signal"
	"syscall"
	"time"
	"github.com/kelseyhightower/envconfig"
	pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
	"work-distribution-patterns/shared/executor"
)
⋮----
"context"
"log"
"net/http"
"os/signal"
"syscall"
"time"
"github.com/kelseyhightower/envconfig"
pubsubinternal "work-distribution-patterns/patterns/p06/internal/pubsub"
"work-distribution-patterns/shared/executor"
⋮----
type config struct {
	BrokerURL        string `envconfig:"broker_url" default:"nats://localhost:4222"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
⋮----
// 1. Setup Transport (Go Cloud PubSub)
⋮----
// 2. Run Worker Loop
⋮----
// Start a simple health check server
</file>

<file path="tests/load/load.go">
// load is a configurable load test tool for the work-distribution-patterns API.
// Run: go run ./tests/load/load.go -url http://localhost:8080
package main
import (
	"encoding/json"
	"flag"
	"fmt"
	"log"
	"math/rand"
	"net/http"
	"strings"
	"sync"
	"sync/atomic"
	"time"
)
⋮----
"encoding/json"
"flag"
"fmt"
"log"
"math/rand"
"net/http"
"strings"
"sync"
"sync/atomic"
"time"
⋮----
type submitRequest struct {
	Name       string `json:"name"`
	StageCount int    `json:"stage_count"`
}
type submitResponse struct {
	ID string `json:"id"`
}
type taskResponse struct {
	ID     string `json:"id"`
	Status string `json:"status"`
}
func main()
⋮----
var (
		submitted int64
		failed    int64
		completed int64
		mu        sync.Mutex
		taskIDs   []string
	)
// Submission loop
⋮----
var sr submitResponse
⋮----
// Wait up to 120s for tasks to complete (server configures stage duration)
⋮----
var tr taskResponse
</file>

<file path="patterns/p05/cmd/api/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"github.com/kelseyhightower/envconfig"
	"github.com/nats-io/nats.go"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/client"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"github.com/kelseyhightower/envconfig"
"github.com/nats-io/nats.go"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/client"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr       string `envconfig:"addr" default:":8080"`
	ManagerURL string `envconfig:"manager_url" default:"http://localhost:8081"`
	NATSURL    string `envconfig:"nats_url" default:"nats://localhost:4222"`
}
func main()
⋮----
var cfg config
⋮----
// Subscribe directly to NATS for events (distributed pub/sub).
</file>

<file path="patterns/p06/internal/pubsub/setup.go">
package pubsubinternal
import (
	"context"
	"fmt"
	"os"
	"strings"
	"time"
	_ "github.com/pitabwire/natspubsub" // Register nats:// scheme with JetStream support
	"gocloud.dev/pubsub"
	_ "gocloud.dev/pubsub/kafkapubsub" // Register kafka:// scheme
)
⋮----
"context"
"fmt"
"os"
"strings"
"time"
_ "github.com/pitabwire/natspubsub" // Register nats:// scheme with JetStream support
"gocloud.dev/pubsub"
_ "gocloud.dev/pubsub/kafkapubsub" // Register kafka:// scheme
⋮----
// OpenManagerResources initializes Go Cloud resources for the manager.
// Returns: tasksTopic (for dispatching), eventsSub (from workers), eventsTopic (to APIs)
func OpenManagerResources(ctx context.Context, brokerURL string) (*pubsub.Topic, *pubsub.Subscription, *pubsub.Topic, error)
⋮----
var tasksTopicURL, eventsSubURL, apiEventsTopicURL string
⋮----
// NATS JetStream URLs
⋮----
// Kafka URLs (connection from KAFKA_BROKERS env var)
// For topics, the path is the topic name
// For subscriptions: kafka://consumer-group?topic=topic-name
// Separate topics to avoid feedback loop: workers→worker_events, manager→api_events
⋮----
// OpenWorkerResources initializes Go Cloud resources for workers.
func OpenWorkerResources(ctx context.Context, brokerURL string) (*pubsub.Subscription, *pubsub.Topic, error)
⋮----
var tasksSubURL, eventsTopicURL string
⋮----
// Workers share tasks via consumer group "workers"
⋮----
// OpenAPIResources initializes Go Cloud resources for API servers.
func OpenAPIResources(ctx context.Context, brokerURL string) (*pubsub.Subscription, error)
⋮----
var eventsSubURL string
⋮----
// NATS JetStream URL (ephemeral consumer)
⋮----
// Each API instance uses its hostname as unique consumer group (auto-scales)
// Docker/K8s automatically assigns unique hostnames like p06-api-1, p06-api-2, etc.
⋮----
// Fallback: generate unique ID if hostname fails
</file>

<file path="shared/contracts/consumer.go">
package contracts
import (
	"context"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"work-distribution-patterns/shared/models"
⋮----
// TaskConsumer is the worker-side transport view.
// Connect establishes the transport; Receive blocks for the next task;
// Emit sends a TaskEvent back to the manager (progress or status).
type TaskConsumer interface {
	Connect(ctx context.Context) error
	Receive(ctx context.Context) (models.Task, error)
	Emit(ctx context.Context, event models.TaskEvent) error
}
</file>

<file path="shared/contracts/producer.go">
package contracts
import (
	"context"
	"errors"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"errors"
"work-distribution-patterns/shared/models"
⋮----
// TaskDispatcher is the manager-side view of the execution substrate.
// Start sets up any transport subscriptions (non-blocking).
// Dispatch enqueues a task for a worker to process.
// ReceiveEvent blocks until an event arrives or ctx is cancelled.
type TaskDispatcher interface {
	Start(ctx context.Context) error
	Dispatch(ctx context.Context, task models.Task) error
	ReceiveEvent(ctx context.Context) (models.TaskEvent, error)
}
// Sentinel errors returned by Dispatch; Manager maps these to HTTP status codes.
var (
	ErrDispatchFull = errors.New("dispatch queue full")  // → 429
⋮----
ErrDispatchFull = errors.New("dispatch queue full")  // → 429
ErrNoWorkers    = errors.New("no workers available") // → 503
</file>

<file path="shared/events/memory.go">
package events
import (
	"context"
	"sync"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"sync"
"work-distribution-patterns/shared/models"
⋮----
// MemoryBridge implements TaskEventBridge using in-memory channels.
type MemoryBridge struct {
	mu   sync.RWMutex
	live map[chan models.TaskEvent]struct{}
func NewMemoryBridge() *MemoryBridge
func (m *MemoryBridge) Publish(event models.TaskEvent)
⋮----
// Notify all live subscribers
⋮----
// Drop slow consumers
⋮----
func (m *MemoryBridge) Subscribe(ctx context.Context) (<-chan models.TaskEvent, error)
</file>

<file path="shared/store/store.go">
package store
import (
	"time"
	"work-distribution-patterns/shared/models"
)
⋮----
"time"
"work-distribution-patterns/shared/models"
⋮----
// TaskStore is the persistence interface for tasks.
type TaskStore interface {
	Create(task models.Task) error
	Get(id string) (models.Task, bool)
	List() []models.Task
	SetStatus(id string, status models.TaskStatus) error
	SetDispatchedAt(id string, t time.Time) error
}
</file>

<file path="patterns/p01/cmd/server/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"time"
	"github.com/kelseyhightower/envconfig"
	"work-distribution-patterns/patterns/p01/internal/goroutine"
	"work-distribution-patterns/patterns/p01/internal/worker"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/executor"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/store"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"time"
"github.com/kelseyhightower/envconfig"
"work-distribution-patterns/patterns/p01/internal/goroutine"
"work-distribution-patterns/patterns/p01/internal/worker"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/executor"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/store"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr             string `envconfig:"addr" default:":8080"`
	Workers          int    `envconfig:"workers" default:"5"`
	QueueSize        int    `envconfig:"queue_size" default:"20"`
	MaxStageDuration int    `envconfig:"max_stage_duration" default:"500"`
}
func main()
⋮----
var cfg config
⋮----
mgr := manager.New(taskStore, dispatcher, bus, 0) // deadline=0 disables re-dispatch
⋮----
// Pump events from the bus into the local hub for the browser
</file>

<file path="patterns/p02/cmd/manager/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"net/http"
	"github.com/kelseyhightower/envconfig"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	restinternal "work-distribution-patterns/patterns/p02/internal/rest"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/store"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"net/http"
"github.com/kelseyhightower/envconfig"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
restinternal "work-distribution-patterns/patterns/p02/internal/rest"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/store"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr             string `envconfig:"addr" default:":8081"`
	WorkersQueueSize int    `envconfig:"workers_queue_size" default:"20"`
}
func main()
⋮----
var cfg config
⋮----
// Pump manager events into SSE hub for API processes to subscribe
⋮----
// Build the router manually to avoid the route collision on POST /tasks
// that api.NewRouter would cause — the manager needs a custom handler that
// accepts a fully-formed Task (not the {name, stage_count} submit request).
⋮----
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck // deprecated but still functional; sufficient for demo
⋮----
// Accept a fully-formed Task forwarded by the API process.
// The API already called models.NewTask(...); we must not create a new UUID.
⋮----
var task models.Task
⋮----
// Worker polling endpoints.
</file>

<file path="patterns/p03/cmd/manager/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"net/http"
	"github.com/gorilla/websocket"
	"github.com/kelseyhightower/envconfig"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	wsinternal "work-distribution-patterns/patterns/p03/internal/websocket"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/store"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"net/http"
"github.com/gorilla/websocket"
"github.com/kelseyhightower/envconfig"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
wsinternal "work-distribution-patterns/patterns/p03/internal/websocket"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/store"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr             string `envconfig:"addr" default:":8081"`
	WorkersQueueSize int    `envconfig:"workers_queue_size" default:"20"`
}
var upgrader = websocket.Upgrader{
	ReadBufferSize:  1024,
	WriteBufferSize: 1024,
	CheckOrigin:     func(r *http.Request) bool { return true },
}
func main()
⋮----
var cfg config
⋮----
// Pump manager events into SSE hub for API processes to subscribe
⋮----
// Build the router manually — the manager accepts a fully-formed Task
// (pre-created by the API with models.NewTask) rather than a submit request.
⋮----
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck
⋮----
// Accept a fully-formed Task forwarded by the API process.
⋮----
var task models.Task
⋮----
// Worker WebSocket registration — workers connect here to receive tasks.
</file>

<file path="patterns/p05/cmd/manager/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"net/http"
	"time"
	"github.com/jackc/pgx/v5/pgxpool"
	"github.com/kelseyhightower/envconfig"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	"github.com/nats-io/nats.go"
	natsinternal "work-distribution-patterns/patterns/p05/internal/nats"
	pgstore "work-distribution-patterns/patterns/p05/internal/postgres"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"net/http"
"time"
"github.com/jackc/pgx/v5/pgxpool"
"github.com/kelseyhightower/envconfig"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
"github.com/nats-io/nats.go"
natsinternal "work-distribution-patterns/patterns/p05/internal/nats"
pgstore "work-distribution-patterns/patterns/p05/internal/postgres"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	Addr        string `envconfig:"addr" default:":8081"`
	NATSURL     string `envconfig:"nats_url" default:"nats://127.0.0.1:4222"`
	DatabaseURL string `envconfig:"database_url" default:"postgres://tasks:tasks@localhost:5432/tasks?sslmode=disable"`
}
func main()
⋮----
var cfg config
⋮----
// Build the router manually — the manager accepts a fully-formed Task
// (pre-created by the API with models.NewTask) rather than a submit request.
⋮----
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck
⋮----
// Accept a fully-formed Task forwarded by the API process.
⋮----
var task models.Task
</file>

<file path="shared/contracts/manager.go">
package contracts
import (
	"context"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"work-distribution-patterns/shared/models"
⋮----
// TaskManager is the single abstraction point between the HTTP layer and
// whatever mechanism is used to execute tasks. All patterns implement this.
//
// TaskManager owns the full task lifecycle:
//   - Persisting the task to the store before dispatching
//   - Dispatching work to the execution substrate (pool, WebSocket, NATS, etc.)
//   - Receiving progress reports from workers and routing them to the event bus
//   - Persisting the final terminal status (completed/failed) to the store
⋮----
// Workers report progress via the TaskConsumer they are given — they have no
// knowledge of stores, event buses, or browsers.
⋮----
// Event streaming is handled separately — each pattern wires events in main.go
// using its native transport (channels, SSE, NATS).
type TaskManager interface {
	Submit(ctx context.Context, task models.Task) error
	Get(ctx context.Context, id string) (models.Task, bool)
	List(ctx context.Context) []models.Task
}
</file>

<file path="shared/events/events.go">
package events
import (
	"context"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"work-distribution-patterns/shared/models"
⋮----
// TaskEventPublisher is the abstraction for broadcasting task events.
type TaskEventPublisher interface {
	// Publish broadcasts an event to all subscribers.
	Publish(event models.TaskEvent)
}
⋮----
// Publish broadcasts an event to all subscribers.
⋮----
// TaskEventSubscriber is the abstraction for receiving task events.
type TaskEventSubscriber interface {
	// Subscribe returns a channel that receives live events.
	// The channel is closed when ctx is cancelled.
	Subscribe(ctx context.Context) (<-chan models.TaskEvent, error)
}
⋮----
// Subscribe returns a channel that receives live events.
// The channel is closed when ctx is cancelled.
⋮----
// TaskEventBridge is a unified interface for both publishing and subscribing.
// It acts as the cross-process or in-memory meeting point for event flow.
type TaskEventBridge interface {
	TaskEventPublisher
	TaskEventSubscriber
}
</file>

<file path="shared/events/nats.go">
package events
import (
	"context"
	"encoding/json"
	"fmt"
	"work-distribution-patterns/shared/models"
	"github.com/nats-io/nats.go"
)
⋮----
"context"
"encoding/json"
"fmt"
"work-distribution-patterns/shared/models"
"github.com/nats-io/nats.go"
⋮----
// NATSBridge implements TaskEventBridge using NATS Core.
type NATSBridge struct {
	nc     *nats.Conn
	prefix string
}
func NewNATSBridge(nc *nats.Conn, prefix string) *NATSBridge
func (n *NATSBridge) Publish(event models.TaskEvent)
func (n *NATSBridge) Subscribe(ctx context.Context) (<-chan models.TaskEvent, error)
⋮----
ch := make(chan models.TaskEvent, 256) // Increased buffer for high concurrency
⋮----
var ev models.TaskEvent
</file>

<file path="shared/sse/hub.go">
package sse
import (
	"encoding/json"
	"sync"
	"work-distribution-patterns/shared/models"
)
⋮----
"encoding/json"
"sync"
"work-distribution-patterns/shared/models"
⋮----
// Hub manages SSE subscribers and broadcasts events to task-scoped or global subscribers.
type Hub struct {
	mu         sync.Mutex
	taskSubs   map[string]map[chan []byte]struct{} // taskID → subscribers
⋮----
taskSubs   map[string]map[chan []byte]struct{} // taskID → subscribers
globalSubs map[chan []byte]struct{}            // taskID="" → all events
⋮----
func NewHub() *Hub
// Subscribe returns a buffered channel that receives SSE event bytes and an
// unsubscribe function. The caller must call unsubscribe when done.
// If taskID is empty, the subscriber receives all events (global subscription).
// Otherwise, the subscriber only receives events for the specified task.
func (h *Hub) Subscribe(taskID string) (chan []byte, func())
⋮----
delete(h.taskSubs, taskID) // reclaim map entry
⋮----
// Publish broadcasts a TaskEvent to task-scoped and global subscribers.
func (h *Hub) Publish(event models.TaskEvent)
func (h *Hub) broadcast(taskID string, data []byte)
⋮----
for ch := range h.taskSubs[taskID] { // range over nil map is safe in Go
⋮----
default: // drop slow consumer
</file>

<file path="shared/store/memory.go">
package store
import (
	"fmt"
	"sync"
	"time"
	"work-distribution-patterns/shared/models"
)
⋮----
"fmt"
"sync"
"time"
"work-distribution-patterns/shared/models"
⋮----
// MemoryStore is a thread-safe in-memory TaskStore used by Pattern 1 and 2.
type MemoryStore struct {
	mu    sync.RWMutex
	tasks map[string]*models.Task
}
func NewMemoryStore() *MemoryStore
func (s *MemoryStore) Create(task models.Task) error
func (s *MemoryStore) Get(id string) (models.Task, bool)
func (s *MemoryStore) List() []models.Task
func (s *MemoryStore) SetStatus(id string, status models.TaskStatus) error
func (s *MemoryStore) SetDispatchedAt(id string, t time.Time) error
</file>

<file path="patterns/p04/cmd/manager/main.go">
package main
import (
	"context"
	"html/template"
	"log"
	"net"
	"net/http"
	"github.com/kelseyhightower/envconfig"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	"google.golang.org/grpc"
	grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
	pb "work-distribution-patterns/patterns/p04/proto"
	"work-distribution-patterns/shared/api"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/manager"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/sse"
	"work-distribution-patterns/shared/store"
	"work-distribution-patterns/shared/templates"
)
⋮----
"context"
"html/template"
"log"
"net"
"net/http"
"github.com/kelseyhightower/envconfig"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
"google.golang.org/grpc"
grpcinternal "work-distribution-patterns/patterns/p04/internal/grpc"
pb "work-distribution-patterns/patterns/p04/proto"
"work-distribution-patterns/shared/api"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/manager"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/sse"
"work-distribution-patterns/shared/store"
"work-distribution-patterns/shared/templates"
⋮----
type config struct {
	HTTPAddr string `envconfig:"http_addr" default:":8081"`
	GRPCAddr string `envconfig:"grpc_addr" default:":9091"`
}
func main()
⋮----
var cfg config
⋮----
// Create gRPC server
⋮----
// Start manager (handles task lifecycle and event routing)
⋮----
// Pump manager events into SSE hub for API processes to subscribe
⋮----
// Start gRPC server in background
⋮----
// HTTP API for compatibility with existing patterns (for remote API to call)
⋮----
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck
⋮----
// HTTP endpoint for API to submit tasks (compatibility with shared/client.RemoteTaskManager)
⋮----
var task models.Task
</file>

<file path="shared/client/manager.go">
package client
import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"time"
	"github.com/labstack/echo/v4"
	"work-distribution-patterns/shared/models"
)
⋮----
"bytes"
"context"
"encoding/json"
"fmt"
"net/http"
"time"
"github.com/labstack/echo/v4"
"work-distribution-patterns/shared/models"
⋮----
// RemoteTaskManager implements contracts.TaskManager by proxying Submit/Get/List
// to the manager process over HTTP. Used by API processes in Patterns 2, 3, and 5.
//
// Events are handled separately — each pattern's API subscribes to events using
// its native transport (SSE for P2/P3, NATS for P5).
type RemoteTaskManager struct {
	baseURL    string
	httpClient *http.Client
}
// NewTaskManager creates a client connected to a Manager process.
func NewTaskManager(baseURL string) *RemoteTaskManager
// Submit sends a fully-formed Task to POST /tasks on the manager.
// Maps 429 and 503 responses to the corresponding echo HTTP errors.
func (m *RemoteTaskManager) Submit(ctx context.Context, task models.Task) error
// Get fetches a single task from GET /tasks/:id on the manager.
// Returns (Task{}, false) on 404 or any error.
func (m *RemoteTaskManager) Get(ctx context.Context, id string) (models.Task, bool)
⋮----
var task models.Task
⋮----
// List fetches all tasks from GET /tasks on the manager.
// Returns an empty slice on any error.
func (m *RemoteTaskManager) List(ctx context.Context) []models.Task
⋮----
var tasks []models.Task
</file>

<file path="shared/models/task.go">
package models
import (
	"fmt"
	"time"
	"github.com/google/uuid"
)
⋮----
"fmt"
"time"
"github.com/google/uuid"
⋮----
var stageNames = []string{
	"Initialization", "Validation", "Processing", "Transformation",
	"Aggregation", "Optimization", "Finalization", "Cleanup",
}
// NewTask constructs a Task with a generated ID, named stages, and pending status.
// stageCount is clamped to [1, 8].
func NewTask(name string, stageCount int) Task
type TaskStatus string
const (
	TaskPending   TaskStatus = "pending"
	TaskRunning   TaskStatus = "running"
	TaskCompleted TaskStatus = "completed"
	TaskFailed    TaskStatus = "failed"
)
// TaskEvent is the single ordered event type flowing from worker to manager.
// Type is either EventProgress or EventTaskStatus.
//
//   - EventProgress events carry StageName and Progress; they are ephemeral
//     (forwarded to SSE clients only, never persisted to the store).
//   - EventTaskStatus events carry Status; terminal statuses (completed/failed)
//     are persisted; "running" is forwarded only.
const (
	EventProgress   = "progress"
	EventTaskStatus = "task_status"
)
type TaskEvent struct {
	Type      string `json:"type"`
	TaskID    string `json:"taskID"`
	StageName string `json:"stageName,omitempty"` // EventProgress only
	Progress  int    `json:"progress,omitempty"`  // 0–100, EventProgress only
	Status    string `json:"status,omitempty"`    // EventTaskStatus only
}
⋮----
StageName string `json:"stageName,omitempty"` // EventProgress only
Progress  int    `json:"progress,omitempty"`  // 0–100, EventProgress only
Status    string `json:"status,omitempty"`    // EventTaskStatus only
⋮----
type Task struct {
	ID           string     `json:"id"`
	Name         string     `json:"name"`
	Status       TaskStatus `json:"status"`
	SubmittedAt  time.Time  `json:"submittedAt"`
	DispatchedAt *time.Time `json:"dispatchedAt,omitempty"`
	CompletedAt  *time.Time `json:"completedAt,omitempty"`
	Stages       []Stage    `json:"stages"`
}
// Stage records the name and index of a task stage.
// Progress is tracked ephemerally via TaskEvent; it is not persisted.
type Stage struct {
	Index int    `json:"index"`
	Name  string `json:"name"`
}
</file>

<file path="tests/e2e/helpers_test.go">
package e2e_test
import (
	"bufio"
	"context"
	"encoding/json"
	"fmt"
	"net/http"
	"os"
	"strings"
	"testing"
	"time"
)
⋮----
"bufio"
"context"
"encoding/json"
"fmt"
"net/http"
"os"
"strings"
"testing"
"time"
⋮----
// baseURL returns the target API base URL (default: http://localhost:8080).
func baseURL() string
// submitRequest is the POST /tasks body.
type submitRequest struct {
	Name       string `json:"name"`
	StageCount int    `json:"stage_count"`
}
// submitResponse is the 202 response from POST /tasks.
type submitResponse struct {
	ID string `json:"id"`
}
// taskResponse mirrors models.Task for JSON decoding.
type taskResponse struct {
	ID     string          `json:"id"`
	Name   string          `json:"name"`
	Status string          `json:"status"`
	Stages []stageResponse `json:"stages"`
}
type stageResponse struct {
	Index int    `json:"index"`
	Name  string `json:"name"`
}
// SSEEvent represents a parsed server-sent event.
type SSEEvent struct {
	Type      string `json:"type"`
	TaskID    string `json:"taskID"`
	StageName string `json:"stageName"`
	Progress  int    `json:"progress"`
	Status    string `json:"status"`
}
// SSEClient connects to the /events endpoint and emits parsed events on a channel.
// If taskID is non-empty, connects to /events?taskID=<taskID> for scoped delivery.
// If taskID is empty, connects to /events for global delivery (all tasks).
// Returns when ctx is cancelled or the connection drops.
func SSEClient(ctx context.Context, t *testing.T, taskID string) <-chan SSEEvent
⋮----
resp, err := http.DefaultClient.Do(req) //nolint:bodyclose // closed in goroutine below or explicit error path
⋮----
var ev SSEEvent
⋮----
// postTask submits a task and returns its ID.
func postTask(t *testing.T, name string, stageCount int) string
⋮----
var sr submitResponse
⋮----
// listTasks fetches all tasks from GET /tasks.
func listTasks(t *testing.T) []taskResponse
⋮----
var tasks []taskResponse
⋮----
// getTask fetches a task by ID.
func getTask(t *testing.T, id string) taskResponse
⋮----
var tr taskResponse
⋮----
// collectedEvents contains the result of event collection.
type collectedEvents struct {
	EventCounts   map[string]int            // eventType -> count (for single task)
	PerTask       map[string]map[string]int // taskID -> eventType -> count (for multiple tasks)
	StatusSeq     []string                  // sequence of status events
	SeenCompleted bool                      // whether task_status=completed was seen
}
⋮----
EventCounts   map[string]int            // eventType -> count (for single task)
PerTask       map[string]map[string]int // taskID -> eventType -> count (for multiple tasks)
StatusSeq     []string                  // sequence of status events
SeenCompleted bool                      // whether task_status=completed was seen
⋮----
// collectEventsUntilQuiet collects events from the SSE channel for the specified task IDs
// until no events are received for the quietDuration. Returns collected event data.
func collectEventsUntilQuiet(ctx context.Context, t *testing.T, events <-chan SSEEvent, taskIDs []string, quietDuration time.Duration) collectedEvents
⋮----
seenEvents := make(map[string]bool) // for duplicate detection
⋮----
// Quiescence reached - all events collected
⋮----
// Filter to our task IDs only
⋮----
// Reset quiescence timer on each new event
⋮----
// Check for duplicates
⋮----
// Track overall event counts (for single task tests)
⋮----
// Track per-task event counts (for concurrent task tests)
⋮----
// Track status sequence
</file>

<file path="shared/api/server.go">
package api
import (
	"html/template"
	"github.com/labstack/echo/v4"
	"github.com/labstack/echo/v4/middleware"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/sse"
)
⋮----
"html/template"
"github.com/labstack/echo/v4"
"github.com/labstack/echo/v4/middleware"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/sse"
⋮----
// NewRouter creates and configures the Echo router with all routes.
func NewRouter(
	hub *sse.Hub,
	tpl *template.Template,
	manager contracts.TaskManager,
) *echo.Echo
⋮----
// Logger skips /health so frequent health-check polls don't flood the log.
e.Use(middleware.LoggerWithConfig(middleware.LoggerConfig{ //nolint:staticcheck // deprecated but still functional; sufficient for demo
⋮----
// Attach template to context for HTMX fragment rendering
</file>

<file path="tests/e2e/task_test.go">
package e2e_test
import (
	"context"
	"testing"
	"time"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)
⋮----
"context"
"testing"
"time"
"github.com/stretchr/testify/assert"
"github.com/stretchr/testify/require"
⋮----
// TestSingleTask verifies a 3-stage task emits progress events and reaches completed.
func TestSingleTask(t *testing.T)
⋮----
const stageCount = 3
// Connect SSE BEFORE submitting task to avoid missing events
⋮----
// Collect events until 1 second of silence
⋮----
// Verify task reached completed status
⋮----
// Executor emits 2 progress events per stage (start + complete)
⋮----
// Executor emits 2 status events (running, completed)
⋮----
// TestConcurrentTasks submits 3 tasks simultaneously and asserts all complete.
func TestConcurrentTasks(t *testing.T)
⋮----
const numTasks = 3
⋮----
// Connect SSE BEFORE submitting tasks
⋮----
// Submit all tasks
⋮----
// Verify event counts for each task
⋮----
// TestStatusTransitions verifies the task emits SSE status events: running → completed.
func TestStatusTransitions(t *testing.T)
⋮----
const stageCount = 2
// Connect SSE BEFORE submitting task
⋮----
// Immediately check pending (task may have been picked up already)
⋮----
// Verify expected status sequence: running → completed
// Note: pending is never emitted as an SSE event (only stored in DB)
⋮----
// Verify event counts
⋮----
// Verify final state in storage
⋮----
var found *taskResponse
</file>

<file path="shared/executor/executor.go">
package executor
import (
	"context"
	"math/rand"
	"time"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
)
⋮----
"context"
"math/rand"
"time"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
⋮----
// Executor runs a task's stages sequentially, emitting progress events.
// MaxStageDuration is set once at construction from server config (MAX_STAGE_DURATION).
// Each stage picks a random duration in [0, MaxStageDuration].
type Executor struct {
	MaxStageDuration time.Duration
}
// Run executes all stages of the task sequentially, emitting a task_status=running
// event at start, a progress event when each stage starts and completes, and a
// terminal task_status=completed or task_status=failed event on exit.
// Context cancellation is detected during each stage's sleep and immediately
// emits a failed status before returning.
func (e *Executor) Run(ctx context.Context, task models.Task, consumer contracts.TaskConsumer)
⋮----
var stageDuration time.Duration
</file>

<file path="shared/api/handlers.go">
package api
import (
	"fmt"
	"html/template"
	"net/http"
	"time"
	"github.com/labstack/echo/v4"
	"work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/sse"
)
⋮----
"fmt"
"html/template"
"net/http"
"time"
"github.com/labstack/echo/v4"
"work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/sse"
⋮----
type submitRequest struct {
	Name       string `json:"name"        form:"name"`
	StageCount int    `json:"stage_count" form:"stage_count"`
}
// SubmitTask handles POST /tasks.
// The task manager is responsible for persisting the task and dispatching it.
func SubmitTask(manager contracts.TaskManager) echo.HandlerFunc
⋮----
var req submitRequest
⋮----
// Return HTML fragment for HTMX requests; JSON for everything else.
⋮----
// ListTasks handles GET /tasks.
func ListTasks(manager contracts.TaskManager) echo.HandlerFunc
// GetTask handles GET /tasks/:id.
func GetTask(manager contracts.TaskManager) echo.HandlerFunc
// SSEStream handles GET /events — streams SSE events to the client.
func SSEStream(hub *sse.Hub) echo.HandlerFunc
⋮----
// Send a comment to flush headers immediately
⋮----
// Heartbeat to keep connection alive
⋮----
// Index handles GET / — serves the HTMX frontend.
func Index(tpl *template.Template) echo.HandlerFunc
// Health handles GET /health — used by Docker health checks.
// Registered before the logger middleware so health poll traffic is not logged.
func Health() echo.HandlerFunc
</file>

<file path="shared/manager/manager.go">
package manager
import (
	"context"
	"errors"
	"net/http"
	"time"
	"github.com/labstack/echo/v4"
	dispatch "work-distribution-patterns/shared/contracts"
	"work-distribution-patterns/shared/events"
	"work-distribution-patterns/shared/models"
	"work-distribution-patterns/shared/store"
)
⋮----
"context"
"errors"
"net/http"
"time"
"github.com/labstack/echo/v4"
dispatch "work-distribution-patterns/shared/contracts"
"work-distribution-patterns/shared/events"
"work-distribution-patterns/shared/models"
"work-distribution-patterns/shared/store"
⋮----
// Compile-time check that Manager implements dispatch.TaskManager.
var _ dispatch.TaskManager = (*Manager)(nil)
// Manager is the unified task lifecycle owner.
// It persists tasks, dispatches them via TaskDispatcher, routes events from the dispatcher
// to the store and event bus, and optionally re-dispatches stalled tasks.
type Manager struct {
	store      store.TaskStore
	dispatcher dispatch.TaskDispatcher
	events     events.TaskEventPublisher
	deadline   time.Duration
}
// New creates a Manager.
// deadline controls re-dispatch: 0 disables the deadline loop entirely.
func New(s store.TaskStore, d dispatch.TaskDispatcher, evs events.TaskEventPublisher, deadline time.Duration) *Manager
// Submit persists the task then dispatches it via the dispatcher.
// Maps dispatcher sentinel errors to appropriate HTTP status codes.
// On any dispatch error, marks the task failed in the store.
func (m *Manager) Submit(ctx context.Context, task models.Task) error
// Get returns the task with the given id from the store.
func (m *Manager) Get(_ context.Context, id string) (models.Task, bool)
// List returns all tasks from the store.
func (m *Manager) List(_ context.Context) []models.Task
// Start initialises the dispatcher then launches the event and deadline goroutines.
// It is non-blocking; call it once before serving requests.
func (m *Manager) Start(ctx context.Context)
// runEventLoop consumes all TaskEvents from the dispatcher in a single goroutine,
// guaranteeing that progress and status events are processed in the order
// they were emitted. Terminal statuses are persisted and all events are
// republished to the event bridge.
func (m *Manager) runEventLoop(ctx context.Context)
// runDeadlineLoop ticks at deadline/2 and re-dispatches tasks whose last
// dispatch time is older than the deadline. Exits immediately if deadline == 0.
func (m *Manager) runDeadlineLoop(ctx context.Context)
</file>

</files>
